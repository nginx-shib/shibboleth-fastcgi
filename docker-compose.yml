version: '3'
services:
  centos-8:
    build: configs/centos-8/
    volumes:
      - .:/app
    environment:
      - _SHIBBOLETH_VERSION
  centos-7:
    build: configs/centos-7/
    volumes:
      - .:/app
    environment:
      - _SHIBBOLETH_VERSION
  centos-6:
    build: configs/centos-6/
    volumes:
      - .:/app
    environment:
      - _SHIBBOLETH_VERSION
